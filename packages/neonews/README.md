# NeoNews APK externo

O APK NeoNews é um artefato proprietário e não deve ser commitado neste repositório.

Para uma validação local, forneça o arquivo neste caminho:

```text
packages/neonews/neonews.apk
```

O pacote esperado é `com.in9midia.neonews.player`, versão `9.0.3` (`versionCode 522`), assinado pelo certificado SHA-256:

```text
220848a28b1de2254c1ac5d3366a64a2f909c265e05dc3495f4b04fcf9cdf91a
```

As ABIs detectadas na auditoria são `arm64-v8a` e `armeabi-v7a`. Consulte [`docs/AUDIT-ETAPA-1.md`](../../docs/AUDIT-ETAPA-1.md) antes de fornecer uma build alternativa.

