<Window x:Class="NeoNews.Runtime.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="NeoNews Runtime"
        Width="1680"
        Height="946"
        MinWidth="1100"
        MinHeight="700"
        Icon="/NeoNewsRuntime;component/Assets/NeoNewsRuntime.ico"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        Foreground="{StaticResource PrimaryTextBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Closing="Window_Closing"
        Loaded="Window_Loaded">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="14" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border Margin="1" Background="{StaticResource WindowBackgroundBrush}" CornerRadius="14" ClipToBounds="True">
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBar" Grid.Row="0" Background="#06111A" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="144" /></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="24,0,0,0" VerticalAlignment="Center">
                <Border Width="30" Height="30" Background="#0C91B3" CornerRadius="6">
                    <TextBlock Text="N" Foreground="{StaticResource PrimaryTextBrush}" FontSize="21" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="NeoNews Runtime" Foreground="{StaticResource PrimaryTextBrush}" FontSize="17" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource ChromeButton}" Click="Minimize_Click" ToolTip="Minimizar"><Path Data="{StaticResource IconMinus}" Stroke="{StaticResource SecondaryTextBrush}" StrokeThickness="1.7" Stretch="Uniform" Width="16" Height="16" /></Button>
                <Button Style="{StaticResource ChromeButton}" Click="Maximize_Click" ToolTip="Maximizar"><Path Data="{StaticResource IconMaximize}" Stroke="{StaticResource SecondaryTextBrush}" StrokeThickness="1.5" Stretch="Uniform" Width="15" Height="15" /></Button>
                <Button Style="{StaticResource ChromeButton}" Click="Close_Click" ToolTip="Fechar"><Path Data="{StaticResource IconClose}" Stroke="{StaticResource PrimaryTextBrush}" StrokeThickness="1.7" Stretch="Uniform" Width="16" Height="16" /></Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="0.175*" /><ColumnDefinition Width="0.825*" /></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid Margin="24,38,24,28">
                    <Grid.RowDefinitions><RowDefinition Height="120" /><RowDefinition Height="*" /><RowDefinition Height="0" /></Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" Margin="20,10,0,0">
                        <Border Width="64" Height="64" Background="#0B2636" BorderBrush="{StaticResource BorderHighlightBrush}" BorderThickness="1" CornerRadius="12">
                            <TextBlock Text="N" Foreground="{StaticResource CyanBrush}" FontSize="39" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Margin="20,7,0,0"><TextBlock Text="NEONEWS" Foreground="{StaticResource PrimaryTextBrush}" FontSize="22" FontWeight="SemiBold" /><TextBlock Text="RUNTIME" Foreground="{StaticResource CyanBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,2,0,0" /></StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Row="1">
                        <ToggleButton x:Name="NavHome" Tag="Home" IsChecked="True" Style="{StaticResource NavItemButton}" Click="Navigation_Click"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconHome}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" StrokeThickness="1.8" Stretch="Uniform" Width="25" Height="25" /><TextBlock Text="Início" Style="{StaticResource NavTextStyle}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" Margin="22,0,0,0" /></StackPanel></ToggleButton>
                        <ToggleButton x:Name="NavRuntime" Tag="Runtime" Style="{StaticResource NavItemButton}" Click="Navigation_Click"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconCube}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" StrokeThickness="1.8" Stretch="Uniform" Width="25" Height="25" /><TextBlock Text="Runtime" Style="{StaticResource NavTextStyle}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" Margin="22,0,0,0" /></StackPanel></ToggleButton>
                        <ToggleButton x:Name="NavNeoNews" Tag="NeoNews" Style="{StaticResource NavItemButton}" Click="Navigation_Click"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconDocument}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" StrokeThickness="1.8" Stretch="Uniform" Width="25" Height="25" /><TextBlock Text="NeoNews" Style="{StaticResource NavTextStyle}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" Margin="22,0,0,0" /></StackPanel></ToggleButton>
                        <ToggleButton x:Name="NavDiagnostics" Tag="Diagnostics" Style="{StaticResource NavItemButton}" Click="Navigation_Click"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconPulse}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" StrokeThickness="1.8" Stretch="Uniform" Width="25" Height="25" /><TextBlock Text="Diagnóstico" Style="{StaticResource NavTextStyle}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" Margin="22,0,0,0" /></StackPanel></ToggleButton>
                        <ToggleButton x:Name="NavSettings" Tag="Settings" Style="{StaticResource NavItemButton}" Click="Navigation_Click"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconGear}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" StrokeThickness="1.5" Stretch="Uniform" Width="25" Height="25" /><TextBlock Text="Configurações" Style="{StaticResource NavTextStyle}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" Margin="22,0,0,0" /></StackPanel></ToggleButton>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="{StaticResource MainContentBrush}"><ContentControl x:Name="PageHost" /></Border>
        </Grid>

        <Grid Grid.Row="2" Grid.Column="1" Background="{StaticResource MainContentBrush}">
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" />
            <Grid Margin="56,0,52,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><TextBlock Text="{Binding LauncherVersionLabel}" Style="{StaticResource FooterTextStyle}" /><Path Data="{StaticResource IconShield}" Stroke="{StaticResource CyanBrush}" StrokeThickness="1.6" Stretch="Uniform" Width="22" Height="22" Margin="32,0,12,0" /><TextBlock Text="{Binding BuildLabel}" Style="{StaticResource FooterTextStyle}" /></StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Path Data="{StaticResource IconClock}" Stroke="{StaticResource SecondaryTextBrush}" StrokeThickness="1.6" Stretch="Uniform" Width="22" Height="22" Margin="0,0,12,0" /><TextBlock Text="Uptime: " Style="{StaticResource FooterTextStyle}" /><TextBlock Text="{Binding UptimeLabel}" Style="{StaticResource FooterTextStyle}" /></StackPanel>
            </Grid>
        </Grid>
    </Grid>
    </Border>
</Window>
